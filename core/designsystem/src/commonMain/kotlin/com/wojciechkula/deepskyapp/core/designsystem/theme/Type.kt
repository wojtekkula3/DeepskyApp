package com.wojciechkula.deepskyapp.core.designsystem.theme

import androidx.compose.material3.Typography

// The old Android app defined no custom typography (relied on Material Components defaults),
// so the Material3 default Typography is the 1:1 baseline.
val DeepskyTypography: Typography = Typography()
